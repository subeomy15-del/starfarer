// Small separable bloom buffers keep the lighting pass lightweight.
function createSpaceEffects(renderer){const size=new THREE.Vector2();renderer.getDrawingBufferSize(size);const make=(w,h)=>new THREE.WebGLRenderTarget(w,h,{type:THREE.HalfFloatType,depthBuffer:false});const main=make(size.x,size.y);main.depthBuffer=true;const blurA=make(Math.ceil(size.x/3),Math.ceil(size.y/3)),blurB=make(Math.ceil(size.x/3),Math.ceil(size.y/3));const scene=new THREE.Scene(),camera=new THREE.OrthographicCamera(-1,1,1,-1,0,1);const vertex='varying vec2 vUv;void main(){vUv=uv;gl_Position=vec4(position.xy,0.,1.);}';
const blur=new THREE.ShaderMaterial({depthTest:false,depthWrite:false,toneMapped:false,uniforms:{source:{value:null},stepSize:{value:new THREE.Vector2()},threshold:{value:0}},vertexShader:vertex,fragmentShader:`varying vec2 vUv;uniform sampler2D source;uniform vec2 stepSize;uniform float threshold;vec3 sampleLight(vec2 p){vec3 c=texture2D(source,p).rgb;return c*max(0.,max(c.r,max(c.g,c.b))-threshold)/max(.001,max(c.r,max(c.g,c.b)));}void main(){vec3 c=sampleLight(vUv)*.227027;c+=(sampleLight(vUv+stepSize*1.384615)+sampleLight(vUv-stepSize*1.384615))*.316216;c+=(sampleLight(vUv+stepSize*3.230769)+sampleLight(vUv-stepSize*3.230769))*.070270;gl_FragColor=vec4(c,1.);}`});
const composite=new THREE.ShaderMaterial({depthTest:false,depthWrite:false,uniforms:{source:{value:main.texture},bloom:{value:blurB.texture}},vertexShader:vertex,fragmentShader:`varying vec2 vUv;uniform sampler2D source;uniform sampler2D bloom;void main(){vec3 c=texture2D(source,vUv).rgb+texture2D(bloom,vUv).rgb*.38;vec2 q=vUv-.5;c*=1.-dot(q,q)*.32;gl_FragColor=vec4(c,1.);
#include <tonemapping_fragment>
#include <colorspace_fragment>
}`});const quad=new THREE.Mesh(new THREE.PlaneGeometry(2,2),blur);scene.add(quad);
return {resize(){renderer.getDrawingBufferSize(size);main.setSize(size.x,size.y);blurA.setSize(Math.ceil(size.x/3),Math.ceil(size.y/3));blurB.setSize(blurA.width,blurA.height);},render(world,view){renderer.setRenderTarget(main);renderer.render(world,view);quad.material=blur;blur.uniforms.source.value=main.texture;blur.uniforms.threshold.value=.85;blur.uniforms.stepSize.value.set(1.5/blurA.width,0);renderer.setRenderTarget(blurA);renderer.render(scene,camera);blur.uniforms.source.value=blurA.texture;blur.uniforms.threshold.value=0;blur.uniforms.stepSize.value.set(0,1.5/blurA.height);renderer.setRenderTarget(blurB);renderer.render(scene,camera);quad.material=composite;renderer.setRenderTarget(null);renderer.render(scene,camera);}};}
